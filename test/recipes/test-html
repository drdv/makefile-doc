-v OUTPUT_FORMAT=html -v COLOR_BACKTICKS=44 -v COLOR_SECTION=0 -v COLOR_DEPRECATED=0 -v EXPORT_THEME=BG:AAAAAA,34:010101 test/Makefile
<span class="color-warning">[test/Makefile] redefined docs of target: test-3</span>
<span class="color-warning">[test/Makefile] redefined docs of target: test-14</span>
<head>
  <style type="text/css">
    .color-attention { color: #dc322f; }
    .color-section {}
    .color-deprecated {}
    .color-default { color: #010101; }
    .color-warning { color: #d33682; }
    .color-backticks { background-color: #268bd2; }
    .makefile-doc-bg { background-color: #AAAAAA; }
  </style>
</head>
<div class="makefile-doc-fg makefile-doc-bg">
<pre>
-----------------------
Available targets:
-----------------------
<span class="color-default">test-1             </span>   test inline docs without top docs
<span class="color-default">test-2             </span>   test inline docs with top docs
<span class="color-default">test-3             </span>   target test-3 is defined twice
                      Spaces before the ## token are allowed and stripped
                      this one will override the one above (and discard its docs)
                        indentation can be used for emphasis
<span class="color-attention">test-4             </span>   use exclamation mark for targets that are signifficant in some way
                      shouldn't have an exclamation mark for subsequent notes

 ----- Section 1 -----

<span class="color-attention">test-5             </span>   but this will be shown instead
test-6                this target is kept just in case
                      even though it is obsolete
                      some note (space befor ## is stripped)
                      note that the above three lines are not dropped
test-7                deprecated target with inline doc
<span class="color-default">test-8             </span>   target test-8 depends on target test-6
<span class="color-default">test-9             </span>   target test-9 depends on target test-6 as well
test-10               this is overridden in Makefile.inc as it is processed second
<span class="color-default">%.c                </span>   here is a pattern rule
<span class="color-default">%.o                </span>   and here is another one
<span class="color-default">bin/%.o            </span>   and yet another one
<span class="color-default">first second third </span>   three targets on one line with spaces between them
<span class="color-default">%.tab.c %.tab.h    </span>   target with dots in the names

 ----- Section 2 -----

<span class="color-default">$(TARGET-NAME)     </span>   target stored in a variable
<span class="color-attention">${TARGET-NAME}     </span>   target stored in a variable
                      make with issue a warning that this target
                      overrides the one above (if they are not empty)
                      but the awk script doesn't detect this becuase it
                      doesn't have access to the value of TARGET-NAME.
<span class="color-default">$(t1) ${t2} t3     </span>   bug from issue #12
<span class="color-default">result-$(1)-$2.html</span>   documentation of a family of targets (with two parameters)
                       + $1: month {jan, ..., dec}
                       + $2: country code {fr, bg, jp, ...}
<span class="color-default">target-with-spaces </span>   target with spaces before and after
<span class="color-default">t                  </span>   A one letter target
<span class="color-default">test-11            </span>
<span class="color-default">test-12            </span>
                      --

<span class="color-default">test-13            </span>   Top description to remain
<span class="color-default">test-14            </span>   An empty line marks the start of a new target
-----------------------
</pre>
</div>
